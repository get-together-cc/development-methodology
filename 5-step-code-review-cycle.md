# 5步法（改→测→审→改→绿）——全员通用代码流程规范

> 用户拍板（2026-08-03）：**所有写代码任务**（不限项目/语言/Agent）必须走此循环。
> **⚡ 与「4步法」的关系**：本 5步法**内嵌在 4步法（备→改→测→记）的②改环节**中；4步法是所有开发任务的强制总入口（见本仓库《四步修改法》）。
> 适用范围：所有 Agent 环境（本机 + 团队协作机器）
> **⛑️ 抢修例外次序**：生产故障先走「抢修通道」——**止血优先于完整修复**（回滚 / kill switch / 降级 → 根因 → 最小修复 → 24h 内补审+记）；完整五步法循环放到服务恢复后执行（通道细则见《四步修改法》「抢修通道」节）。
> 最后更新: 2026-09-24（新增：抢修例外次序 + UI 视觉验证 DOM×截图交叉）

## 核心定义

```
① 改 → ② 测 → ③ 审 → ④ 改 → ⑤ 绿    （③④ 循环直到无新问题才进 ⑤）
```

## ⚠️ 最重要规则：循环次数不限（但有收敛规则）

- **没有"固定 5 轮"**——轮数由问题数量决定，不设上限
- **终止条件**：某轮只剩误报/无新问题 → 才进 ⑤ 绿
- **禁止提前停**：不得因"跑了几轮差不多了"就宣布全绿
- **每轮结束主动问自己**："这轮还有 critical/major 吗？有就继续，没有才停"

### 🔀 收敛规则（2026-08-04 评审建议，已采纳）

"次数不限" ≠ "无限打磨"。加两条收敛规则平衡：

1. **连续 2~3 轮无实质改动（或只剩风格/优化类意见）→ 停**
   - 每轮记录改动量，连续 2-3 轮无实质改动即视为收敛完成
2. **续循环入场券：只接受正确性 / 安全 / 性能 三类硬问题**
   - 风格类、优化类意见**不构成再开一轮的理由**（记入待办即可，不阻塞交付）

**与"不限次数"的关系**：次数不限但**有实质进展才算数**——只要每轮都在修硬问题就继续；只剩风格意见就停，不无限打磨。

## 五步详解

### ① 改
- 遵守代码同步铁律：先拉最新 → 本地改 → 语法检查 → diff 确认 → 上传
- 一次只改一个根因，不做 drive-by 重构

### ② 测
- **测试标准不确定时必须先问用户确认**（测什么范围/对比什么/什么算通过）
- 实际调用/运行验证：全端点 curl，确认 200 + 产物完整可读
- **与旧版输出对比**（有原版必对比）：Excel 逐单元格、PDF 双标签正则、排序按原版逻辑
- 回归：已验证的基线必须重跑确认没改坏
- **UI 视觉验证 = DOM×截图交叉**（必做）：DOM 断言（computed / 几何 / elementFromPoint / 文本）与截图判读（视觉识别 / 像素采样 / OCR）**双证对账，缺一不可**；单证「通过」不算通过；不一致 → 以可复现硬证据为准并查明原因（细则见《视觉确认检查表》）

### ③ 审（审核视角 —— 多模型交叉审核，4 路并行）

**当前生效矩阵（2026-09 起；执行细节以内部审核规范 `multi-model-code-review` 为唯一权威）**：

| 路 | 模型 | 状态 |
|----|------|------|
| A | 云端主模型（DeepSeek Flash，官方直连） | ✅ 必跑（逻辑链 bug + 主动实测）|
| B | 本地自托管模型（FreeToken 35B，**统一出口**，后端模型切换对客户端透明）| ✅ 必跑（深度细节/TDZ/命名空间）|
| C | 云端主模型 · 测试视角 | ✅ 必跑（数组注入/并发/状态组合，独有发现 ~70%）|
| E | 本地模型 · 测试视角 | ✅ **必跑第 4 路**（2026-09-06 起默认启用；抓集成/运维类：API 限流/honeypot/脚本硬编码）|
| D | 云端增强路（DeepSeek Pro） | ⏸️ 暂停中（2026-09-04 起）|

**命令**（cd 代码目录后发车，各写独立 log；模型标识/端点按各自环境配置）：

```bash
opencode run -m deepseek/deepseek-flash --variant max '审核prompt' > /tmp/review-cloud-cr.log 2>&1 &
opencode run -m freetoken/4080-local '审核prompt' > /tmp/review-local-cr.log 2>&1 &
opencode run -m deepseek/deepseek-flash --variant max "$(cat <测试视角prompt>)" > /tmp/review-cloud-test.log 2>&1 &
opencode run -m freetoken/4080-local "$(cat <本地测试视角prompt>)" > /tmp/review-local-test.log 2>&1 &
# 云端增强路暂停中：opencode run -m deepseek/deepseek-v4-pro --variant max '...' &
# 云端两路 2026-09-18 起对齐主模型；本地两路走统一出口（2026-09-22 起全量对齐）
```

- **按模块分批并行审**（1-2 文件/批），一次全量会挂死
- prompt 必须带上一轮已修清单（"第一轮已修复: X/Y/Z。请验证修复是否正确"）
- 要求：中文输出、Severity critical/major/minor + 行号 + 修复建议、只审不改
- **本地模型测试视角（黑盒实测）**：不做代码阅读，重点测数组/批量参数注入、越权、限流伪造（X-Forwarded-For）、状态标记绕过、边界值；发现必须带复现证据（请求+响应）；修复后必须重测确认
- **统一出口纪律**：① 自写 curl 单发必须带 `"stream": true`（非流式会被代理零字节超时切断）② 同一后端**任一时刻只处理 1 个生成**——本地两路**顺序**发，勿并发饿死 ③ 支持全程实时观看（`tail -F` 各路日志）
- **沙箱纪律**：OpenCode 只能读写工作目录；目录外路径触发 auto-reject，且 **auto-reject 会静默杀死整个会话**（无报错、进程消失）——参考文件预放进项目目录（如 `core-refs/`），题面写「沙箱须知：只读写项目目录，不要碰 /tmp 与家目录」；文件总量小时**直接全文内嵌 prompt**（省读取、绕权限、防游走）
- **⚠️ 实证 ≠ 免审**：影子双跑/用例矩阵等实证类验证只是②测的加强，**不能替代③审**——实证一致只证明"与原实现行为相同"，证明不了"新代码自身无缺陷"
- ⚠️ 版本归一说明：本文档与内部 `code-change-review-cycle` 技能及 GitHub `code-review-cycle` 仓库均以内部审核规范（`multi-model-code-review`）为唯一权威同步。变更史：08-21 3模型 → 08-30 +测试视角 → 09-04 Pro 暂停(3路) → 09-06 本地测试视角默认启用(4路必跑) → 09-18 云端两路对齐主模型 → 09-20 统一出口增补（流式/并发/沙箱纪律）→ 09-22 统一出口升为标准 ID

### ④ 改
- 修 critical/major；minor 评估后决定
- **误报判定三件套**：grep 符号（0 匹配）+ 编译通过 + git diff 确认——都过就是误报，不盲改
- **理论建议 vs 实证冲突**：AI 报理论问题时，若与旧版导出已 0 差异 = 行为符合原版，以实证为准不改，加注释说明

### ⑤ 绿（终止条件）
- 某轮只剩误报/无新问题 → 终止
- 全部修复部署后跑最终回归（全端点 + 数据对比基线）→ **全绿才交付**

## 全绿的完整含义（双重）

1. **功能测试全绿**：全端点 200 + 与旧版数据逐单元格 0 差异 + 批量 0 错误 + 服务 0 崩溃
2. **复审全绿**：多轮交叉复审无 critical/major 遗留（实际项目有 7 轮复审经验）

## 配套纪律

- **新问题点必须固化**：每次遇到新问题 → 修复 → 验证 → 立即加入 TESTING.md 回归清单
- **review 运行期间不要改文件**（AI 读启动时快照，中途修改导致误报）
- **修复后全局 grep 同类模式**（修 1 处还要找漏网）
- **测试文档随流程更新**：每条测试 = 方法 + 预期 + 状态，执行后回填

## 各 Agent 执行要求

所有 Agent（本机 + 团队协作机器）统一按本文档执行：

- 写代码任务必须走 5步法；③审统一用 OpenCode CLI（不换其他审查工具、不跳过）
- 涉及安全的改动：安全审查结果按 severity 分级，与五步法结论一并闭环
- 已把流程固化进技能/记忆的 Agent 自动执行，无需人工提醒

## 验证标准（怎么算执行正确）

- ✅ 循环次数 ≥ 问题所需轮数（不限，直到无新问题）
- ✅ 每轮审核都带上一轮已修清单
- ✅ 误报有判定证据（grep/编译/diff 三件套）
- ✅ 最终回归全绿才交付
- ❌ "做了 3 轮差不多了"提前停 = 违规

## 相关文件

- 参考实现（本机技能）: `~/.hermes/skills/software-development/code-change-review-cycle/SKILL.md`
- 审核编排细节（批量发车/权限坑/流式排查）: 见配套 `opencode-review-orchestration` 参考文档
