# 五步代码审查循环

## 概述

可执行代码修改的核心内循环，嵌套在四步法的「②改」之中。不是一次性走完五步就完事——**审和改是个循环，不限次数，直到收敛**。

```
改 → 测 → 审 → 改 → 绿（重复 审↔改 直到无新问题）
```

## 五步

### 第一步：改（+ git）

- 最小化改动
- 带 git commit（提交信息清晰）
- 版本号双保险（文件头 Version + 常量定义）
- CHANGELOG 同步更新

### 第二步：测

- 测试范围**先于代码列全**（见「测试策略决策框架」）
- 写成 TESTING.md：方法 + 预期 + 状态，执行后逐条回填
- 类型：语法检查 → 逻辑测试 → UI 测试 → DB 验证 → 基线回归

### 第三步：审

- 多模型并行审核（见「安全审计清单」辅助提示词）
- 聚焦：逻辑错误、边界条件、安全、命名一致性、凭据泄露

### 第四步：修

- 每个 Critical/Major 发现必须修
- 已修项标记「再审确认」
- **必须回第二步重跑回归**——每一次修改都需要重新验证

### 第五步：绿（终止条件）

- 当前轮只剩误报、或连续 2-3 轮无实质修复 → 终止
- 最终回归：重跑所有之前通过的测试用例
- 部署前再过「交付前清单」（版本/CHANGELOG/缓存/基线/审核闭环）

## 三回路嵌套模型

```
                    ┌────────── 测试回路（快）─────────────┐
                    ↓                                     │
改 → 测 ──失败──→ 回改                                   │
   ↕      ↓通过                                         │
   │     审 ──发现问题──→ 修 ──→ 回测（回归）──────────────┘
   │       ↓ 无新问题                                    │
   │      绿（全量回归确认）                               │
   └──────────── 整体大循环：部署前再整体走一遍 ────────────┘
```

| 回路 | 速度 | 触发 | 职责 |
|---|---|---|---|
| **测↔改（快）** | 快 | 测试失败 | 快速红绿反馈 |
| **审↔修（慢）** | 慢 | 审核发现 Critical/Major | 代码质量门禁 |
| **修→回归（中）** | 中 | 每次修改 | 保证修复不搞坏已有的 |

**关键纪律**：每次「修」之后必须回「测」跑回归。靠旧测试结果当「修好了」是不算数的。

## 分级路径

| 改动类型 | 流程重量 | 测试要求 |
|---|---|---|
| 可执行代码（PHP/JS/Go/逻辑） | 完整五步法 + 多模型审核 | 逻辑测试 + 全端点 + 回归 |
| CSS/纯样式 | 轻量（跳过多模型审核） | UI 截图确认 + 自适应断点 |
| 文案/配置单值 | 最轻 | 页面确认 + DB/文件复查 |

## 关键规则

- **部署后才审就晚了**：所有审核闭环必须在部署前完成
- **交付总结附审核清单**：发现问题数（Critical/Major/Minor）、已修数、再审确认、基线复验——只写「全部完成」不算走完五步法
- **UI 必须真测**：headless 浏览器真实点击，不是 curl 模拟 POST 或只看 DOM