# 自研交付物 安装/升级/卸载 三清单规范(2026-09-08 用户拍板)

> 状态: 规范 v1.0, 已挂接技能(code-change-review-cycle 引用本文件为权威源)
> 范围: 所有自研交付物——WP 插件、mu-plugin、主题、Go 微服务/systemd 服务、PHP 框架站自定义系统、客服系统等任何**多文件、多设置点**的代码
> 定位: 与「备→改→测→记」4步法互补但不同范围——4步法管改动安全(备份), 本规范管**交付物生命周期**(装/升/卸的记账与清理)

## 问题(2026-09-08 用户提出, 有实证)

1. **升级不清理旧版本** → 旧文件/旧定时任务残留, 与新版本冲突(ft-guard 旧版复活覆盖 2026-09-04 二次复发; 服务器 .bak 残留; cron 任务更新后旧的仍跑)
2. **卸载不干净** → 残留文件/DB/options/cron/hook 影响系统或其他程序

## 核心: 三清单 + 记账原则

| 清单 | 何时维护 | 内容 | 解决 |
|---|---|---|---|
| **安装清单**(install manifest) | 开发/装版时 | 创建了什么: 文件清单、DB 表、options/设置项、cron/定时任务、hook、缓存 key、systemd unit、目录 | 记账——卸载时知道要反做哪些 |
| **升级迁移**(upgrade delta) | 每次发版 | vN→vN+1 变更 + **废弃项移除段**(旧版引入、新版不再用的文件/钩子/任务) | 旧文件/旧任务冲突 |
| **卸载清单**(uninstall manifest) | 开发时维护 | 安装清单逆操作 + 运行时产物(日志/缓存/临时数据) | 卸载干净 |

**记账原则（2026-09-08 用户精化）**: 安装清单分两层, 不为记录而记录——
- **标准产物**: 安装流程按模板创建的常规项(文件清单/systemd unit/标准 options/dbDelta 表), 开发时一次写清在清单里即可, 无需运行时逐项记
- **例外追加**: 任何**非常规安装/运行时创建物**——手跑 SQL 建的表、手动加的 cron、旁路脚本创建的文件、手工 mkdir 的数据目录、临时改的配置——**操作当时就追加进安装清单**(带日期+原因)。残留事故几乎都来自这类"清单外创建物"
- 卸载时 = 清单反做; 升级时 = 对照 delta 清废弃

**⚠️ 所有权标注 + 引用检查（2026-09-08 用户追加: 避免过度删除）**: 清单每项标注所有权——
- **独占(owned)**: 本系统独有, 卸载可删
- **共享(shared)**: 其他系统也在引用(公共目录/共享函数/跨插件 options/共用 cron/公共缓存 key/公共表), **删除前必须先做引用检查**(grep 谁还在引用该文件/option/hook/cron/表), 有引用者 → 不删, 记录"共享保留"并在卸载说明注明; 无引用才可删
- 删除顺序: 先断自己的引用(停 hook/移除 cron), 再执行删除; 共享项一律"留+注明", 不赌别人没用

## 第一问: 保留数据还是不保留(升级决策树)

```
升级/卸载前先判: 本交付物有没有用户数据/设置?
├─ 有(DB 表内容/options 设置/业务数据) → 保留型:
│    升级 = 兼容升级 + 增量迁移(版本号存 options/表, delta 迁移)+ 废弃项清理
│    ✗ 禁止"卸载重装"——会丢数据/设置(事故!)
│    升级前走 4步法①备份(另一范围, 但保留型必须备份)
├─ 无(纯文件/无状态) → 不保留型:
│    升级 = 按安装清单清旧文件 → 部署新版 → 重启/验证
│    卸载 = 卸载清单全清
└─ 混合(文件型+少量状态) → 状态部分按保留型, 文件部分按不保留型, 分而治之
```

## 按交付物类型落地

| 类型 | 安装记账 | 升级 | 卸载 |
|---|---|---|---|
| WP 插件 | 代码注释 + 版本 options | `uninstall.php` 不存在时用版本号+迁移函数: dbDelta/options 迁移 + 废弃 hook/cron 移除 | 标准 `uninstall.php`(register_uninstall_hook): 删 options/表/自定义 cron——**未接卸载钩子的自研插件 = 欠账** |
| Go 微服务 / systemd | systemd unit + 文件清单 | 新二进制原子替换(scp/mv 已有模式)+ 按清单清旧文件 | `UNINSTALL.md`: stop+disable unit → 删 unit/文件/数据目录/日志 → 删 cron |
| PHP 框架站自定义系统 | 文件清单 + DB 变更记录 | 文件清旧装新 + DB delta | 文件清单逆操作 + DB 对象清单 |
| 主题 | 文件清单(注意版本化资源?ver=) | 清旧文件 → 装新(保留用户设置则 options 迁移) | 删文件 + 删主题 options |

## 与现有流程的接缝

- **执行时机(2026-09-08 用户拍板)**: 三清单挂在 4步法上——新项目**首次 4步法即建立安装清单**, 此后每次发版/升级走 4步法时同步维护 upgrade delta; 卸载/清理按卸载清单+所有权检查执行(server-change-4step 已挂接)
- **4步法(备改测记)**: 另一个范围(改动安全)。保留型升级前必须走①备份; 本规范不替代备份
- **代码头 VERSION+CHANGELOG**(2026-09-05 规范): CHANGELOG 每版注明「移除: xxx」(废弃项), 与 upgrade delta 对应
- **部署四步(scp→wc→mv→md5)**: 升级部署显式加一步「按上一版安装清单清废弃文件/任务」(清旧→装新→验证)
- **代码版本号铁律**: 卸载/迁移逻辑随版本号分支, 不能只靠"当前文件存在与否"判断

## 待办(盘点欠账)

- [ ] 盘点现有自研件: gt-* 插件(gt-expert-pass/ppr-redirect-lite/gt-woo-import/gt-showroom 等)、Go 服务(ft-guard/hb-heartbeat/gt-woo/export-orders)、客服系统(server.py/ask.py)、主题——哪些缺 uninstall.php/UNINSTALL.md/版本迁移
- [ ] 盘点结果 → 分优先级补齐(有状态且可能卸载的优先)
