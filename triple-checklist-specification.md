# 三清单规范——安装/升级/卸载

> 所有自研交付物：WP 插件、mu-plugin、主题、Go 微服务、systemd 服务、PHP 自定义系统、客服系统等任何「多文件、多设置点」的代码，都必须有三清单。
> 与「备→改→测→记」四步法互补但不同范围——四步法管**改动安全**（备份），本规范管**交付物生命周期**（装/升/卸的记账与清理）。

## 三清单定义

### 1. 安装清单（install_manifest）

**时机**：新项目首次「四步法·②改」时建立。

记录此交付物创建了哪些东西——卸载时要全部反做：

- [ ] 文件清单：所有新增文件
- [ ] 数据库变动：表/字段/索引
- [ ] WordPress options：选项 key 列表
- [ ] 定时任务：cron hook 名
- [ ] 自定义钩子：filter/action 命名
- [ ] 自定义文章类型/分类法：post type / taxonomy 名
- [ ] 缓存键：redis/memcached key 前缀
- [ ] 非常规操作：修改系统文件、增加环境变量、安装外部依赖

**记账规范**：
- 标准产物（脚手架生成的类/模板/控制器等）在开发时写清即可
- 非常规操作（手动修改系统文件、安装外部依赖、开端口等）**必须当场追加**到清单，事后容易忘

### 2. 升级增量清单（upgrade_delta）

**时机**：每次发版/升级时同步维护。

对比上一个版本，记录「增/删/改」三态，含废弃文件/定时任务/选项的**移除段**：

```yaml
1.19.0:
  changed:
    - product-batch-edit.php: 新增预览→提交链逻辑
    - order-sku.php: 推单优先级链新增 JST SKU 优先
  removed:  # 废弃段——重要！不删则旧文件/旧定时器留服务器
    - old-function.php
    - gt_legacy_option
    - cron: gt_daily_cleanup (→ 已迁移到新名)
```

### 3. 卸载清单（uninstall）

**时机**：系统/插件需要整体卸载时。

按「安装清单」反做：

- [ ] 每个 cron hook 调用 wp_clear_scheduled_hook 移除
- [ ] 每个 option 用 delete_option 清理
- [ ] 自建 DB 表 DROP
- [ ] 删除文件

## 所有权标注（防过度删除）

安装清单的每一项标记**独占**或**共享**：

- **独占**：此交付物独有的文件/选项/hook，卸载时直接删
- **共享**：多个系统共用的文件/选项（如共同的数据表、共享库），**删除前先 grep 检查引用**——有引用则只注明「不再创建」，不删

## 升级/卸载流程

### 升级前第一问（2026-09-08 用户拍板）

**「保留数据还是不保留？」**

- 保留数据：只覆盖文件，不动数据库表/option
- 不保留数据：清空后全新装上

此决策直接影响 upgrade delta 的写法——保留模式不写 DROP/delete_option。

### 卸载前所有权检查

共享项删除前必须确认无其他系统引用：

```bash
grep -r "shared_option_key" /path/to/codebase
# 有引用 → 只标注「不再创建」，不删
# 无引用 → 正常删除
```