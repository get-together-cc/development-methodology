# 视觉确认检查表（UI/样式改动的验证清单）

> 任何「UI 看起来对不对」的改动，按本表验证。**主规则：DOM × 截图交叉验证**——双证对账，单证「通过」不算通过。
> 与《测试策略决策框架》配套：先定「测什么」，再按本表把视觉类测到「算数」。

## 0. 总原则

1. **证据链**：DOM 断言（是什么 / 在哪）× 截图（画成什么样）× 像素与 OCR（可复现的硬数）× 视觉模型（语义判读）× 人工目检（观感裁决）。最低要求 **DOM + 截图双证**。
2. **机器判量值，人判观感**：computed / 像素 / 几何 = 机器判；审美 / 协调 / 真机观感 = 人判。
3. **口径分清**：结论里明确列「已客观核对」与「待目检 / 待真机」两栏，不混说。

## 1. 测前检查

- [ ] **断言清单**：本次要验证的断言（元素 / 尺寸 / 颜色 / 位置 / 遮挡 / 断点）先列出，不凭感觉扫一眼
- [ ] **视觉通道可用性**：视觉模型不可用时走降级口径（computed + 像素采样 + 交人工目检），不阻塞交付
- [ ] **截图规划**：全页 + 关键元素局部各一份；大图先降采样（约 1300px 宽）再送视觉模型
- [ ] **会动的元素**：暂停动画 / 等页面稳定再截（动画中的 transform 会干扰判定）

## 2. 采集（双证）

- [ ] **DOM 证**：computed 样式 / getBoundingClientRect() 几何 / document.elementFromPoint(x, y) 遮挡与层叠 / 文本内容
- [ ] **截图证**：全页 + 局部截图 / 像素采样（关键断言取色、量距）/ OCR（文案与编号）
- [ ] **缓存前提**：改了静态资源先 bump 版本参数再截（否则截到旧缓存画面）

## 3. 判定（对账）

- [ ] 两证一致 → 通过
- [ ] 两证不一致 → **以可复现硬证据为准**（像素 / computed / elementFromPoint / 实测），并查明差异原因（对照 §4）
- [ ] **视觉模型结论按「描述」对待**：它描述「看到什么」，不判「是什么身份 / 真假」——身份、层叠、归属类结论必须由 DOM 侧交叉

## 4. 六类已知失效模式（对账台账）

| # | 失效模式 | 症状 | 对策 |
|---|---|---|---|
| 1 | 指令聚焦偏差 | 只答问的、不主动报告遮挡 | 提示词**双问题**（「有无遮挡？」+「整体描述」） |
| 2 | 判宽度 / 外观误报 | 白底区域被误判「仍有限宽」类 | 改用**像素测量**为准；视觉模型只做定性 |
| 3 | 元素身份 / 层叠误判 | 把被覆盖的独立元素读成「在另一栏里」 | elementFromPoint × DOM × 截图 × OCR **交叉** |
| 4 | 上下文限制拒收大图 | 超宽截图被拒（模型上下文有限） | 先降采样再送；原图仍按原样归档 |
| 5 | 真机 / iOS 差异 | 桌面与无头正常，真机渲染不同 | 真机 = **人判项**；交付附「真机待验」栏 |
| 6 | 视觉通道不可用 | 算力被占 / 排队 / 超时 | 降级：computed 数值 + 交人工目检 + **口径分清** |

## 5. 人判项（不交给机器）

- 审美 / 风格 / 协调性、物理观感（透明度、玻璃感等）、真机观感 → 一律标 **人判项**。
- 交付时列「**已客观核对**」+「**待目检 / 待真机**」两个清单。

## 6. 测后收尾

- [ ] 关键截图按项目规范归档（与测试文档同目录，不落临时目录）
- [ ] 测试数据清理还原（构造的状态 / 字段改回）

## 相关

- 测什么 / 怎么测：《测试策略决策框架》
- 部署前逐项确认：《交付前清单》
