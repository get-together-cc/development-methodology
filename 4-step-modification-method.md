# 四步修改法

## 概述

任何代码修改或新功能开发的**强制总入口**。需求定完，自动从这里开始——不需要人提醒「走流程」。

四步：**备 → 改 → 测 → 记**。可执行代码的②改环节内嵌「**5步法**：改→测→审→改→绿」（详见《五步代码审查循环》）。

## 四步

### 第一步：备份与准备（备）

- **先备份**：数据库导出、文件复制、git 基线提交
- **明确范围**：改什么？影响谁？要测试什么？
- **环境选择**：本地 → 模拟站 → 生产

### 第二步：修改与验证（改）

- 最小化改动原则——只改该改的行，不做无关优化
- **嵌套五步法**：可执行代码的修改在此步内走「改→测→审→改→绿」循环
- **分级路径**（按改动类型自动分配流程重量）：

| 改动类型 | ②改 流程 | 测试要求 |
|---|---|---|
| 可执行代码（PHP/JS/Go/逻辑） | 完整五步法 + 多模型审核 | 逻辑测试 + 全端点 + 回归 |
| 鉴权/安全边界改动（单入口文件） | 完整五步法（侧重安全视角） | **影子双跑**（同请求并行新逻辑，对比一致率）+ 用例矩阵（角色×凭据状态×攻击路径×边界）+ fail-closed 收紧 |
| 存量契约变更（行为故意改变：有效期/权限/计费规则） | 完整五步法 | **全量回放**（生产全量存量数据喂新逻辑，验证零误伤）+ 边界用例矩阵 + kill switch + 上线后真实链路验证 |
| **抢修（生产故障/服务中断）** | **止血优先**：先恢复服务（既有回滚开关/降级/隔离），完整修复不塞进故障窗口（见「抢修通道」节） | 止血动作**当场实测**（服务真恢复才算）；热修 = 最小 diff + 可回退 + 回归 |
| CSS/纯样式 | 轻量（跳过多模型审核） | UI 视觉确认（**DOM×截图交叉**）+ 自适应断点（必测） |
| 文案/配置单值 | 最轻 | 页面确认 + DB/文件复查 |

⚠️ 分级不等于免测：样式改完必须看 UI + 测断点自适应。

### 第三步：测试验证（测）

- 每个改动必须**实际验证**（curl / 日志 / 浏览器 / 数据对比）
- 有 UI 的功能必须用无头浏览器**真实点击**，curl 查 DOM 不算
- **UI 视觉结论必须 DOM × 截图交叉对账**：DOM 断言（computed/几何/遮挡判定）与截图判读（视觉识别/像素采样）缺一不可，单证「通过」不算通过；不一致时以可复现硬证据为准并查明原因（细则见《视觉确认检查表》）
- 测试范围按「测试策略决策框架」先列好再动手，不是 bug 补集
- **验证方法选型（等价 vs 行为变更）**：
  - 语义等价替换（重构/性能优化）→ **影子双跑**：新旧并行，比一致性率（危险信号 = 不一致）
  - 行为变更（规则/契约故意改变）→ **全量回放**：一致性指标失效（预期就不一致），比「新规则 ⇄ 预期行为」（危险信号 = 误伤——该过的被拒）
- 实证类验证（影子双跑/用例矩阵/全量回放全绿）**不能替代多模型审核**——实证只证明「与原实现行为一致/符合预期」，证明不了「新代码自身无缺陷」

### 第四步：记录归档（记）

- 维护记录三步：README 审计日志 + 任务总结 + 索引入知识库
- 三清单同步更新（安装/升级/卸载）
- 交付总结附审核结果清单，不写「全部完成」

## 抢修通道（止血优先于完整修复）

> 适用：生产故障 / 服务中断。顺序铁律：**先止血，后修复**——完整修复不在故障窗口里做。

```
止血 ─→ 根因 ─→ 最小修复 ─→ 记（24h 内补齐）
```

| 步 | 做什么 | 纪律 |
|---|---|---|
| ① 止血（第一优先） | 尽快恢复服务：既有 **kill switch / 回滚点** > 降级/隔离（关功能、切流量、停任务）> 临时热修 | 取最确定的动作，不追求完整/漂亮；**当场实测**服务已恢复；动前留现场快照 |
| ② 根因 | 服务恢复后再定位（日志 / 时间线 / 取证） | 不在故障窗口里深挖 |
| ③ 最小修复 | 热修 = 最小 diff + 可回退 + 回归测；完整修复留到恢复后按正常流程走 | 抢修不豁免审核——热修事后补审核轮 |
| ④ 记 | 24h 内补齐：完整修复（若热修是权宜）+ 审核 + 记录归档 | 止血 ≠ 结案——哪怕只翻了一个开关也要记 |

红线（不因抢修放宽）：不能影响现有用户状态（令牌 / 会话 / 存量数据类先给零影响证据并确认）；破坏性动作先确认；共享环境先确认无人在用。

## 关键规则

- **总入口铁律**：每个开发任务自动从第一步开始，不等用户喊「走流程/五步法」
- **验证铁律**：「改完了」≠「做完了」——必须通过实际测试验证
- **生产不跑测试**：生产只观察，不部署测试数据。用模拟站/本地回放代替
