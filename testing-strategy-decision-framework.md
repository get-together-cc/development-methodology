# 测试策略决策框架（测什么·怎么测才正确）

> 背景：一次产品批量更新页面的开发中，两个 UI 相关 bug 漏到了生产——根因是「测什么」没有显式的决策框架，只验证了渲染，没做交互路径测试。本框架把测试范围决策显式化，每个改动先列测试范围再动手。

## 核心原则

**「怎么测」已有铁律；「测什么」必须也有框架。** 每个改动按三个维度定测试范围，先写成 TESTING.md（方法 + 预期 + 状态）再动手，不是用户报 bug 的补集。

## 维度一：改动影响面（先问「改了啥，影响谁」）

| 改动类型 | 影响范围 | 测试必覆盖 |
|---|---|---|
| 文件增删改 | 调用方/端点的 200/404/403 | curl 全端点 + 脚本执行检查 |
| 商业逻辑 | 所有分支路径 | 枚举：正常 + 错误 + 边界 + 并发 |
| UI 模板/JS | 用户可见 + 交互行为 | 按钮点击链 + 弹窗 + 状态切换 |
| CSS/样式 | 渲染效果 + 设备适配 | 截图对比 + 自适应断点 |
| 数据库/存储 | 业务数据一致性 | SELECT 复查 + 新旧版本对比 + 幂等验证 |

## 维度二：路径枚举（不止测「做了」，要测「全路径」）

```
每项改动按此模板列测试范围：
① 主路径（正常成功）          → 1 条
② 错误路径（验证/权限/格式/异常）→ 每个守卫 1 条
③ 边界值（空/零/超限/重复/类型不匹配/缺字段）→ 每边界 1 条
④ 并发/幂等（同条件多次提交/并行请求）→ 要求验证，不轻易降级
⑤ 交互组合（多步骤交叉/取消→重来/预览→执行→再预览/按钮默认行为）→ UI 全路径
⑥ 回归（基线上次通过的用例重跑）→ 确认没搞坏
```

**常被漏的类型（教训）**：④⑤ 经常被跳过——预览→执行的组合路径、按钮不带 type 的默认 submit 行为、二次上传的残留清理。

## 维度三：正确的方法（每种测试怎么测才算数）

| 要测什么 | 正确方式 | 不算数的 |
|---|---|---|
| 服务端逻辑 | 语法检查 + CLI eval + curl 全端点 + DB 复查 | 只看代码 |
| 商业逻辑 | 路径枚举 + 真实数据对比（新旧逐字段） | 手工构造假数据 |
| UI 渲染 | DOM × 截图交叉对账 + 像素级裁决 | 只看 DOM 里有字段（或只看截图） |
| UI 交互 | 无头浏览器 CDP 真实坐标点击 | curl 模拟 POST / 只看 HTML |
| 自适应 | 多 viewport 截图对比 | 单宽度测试 |
| 并发/幂等 | 脚本构造同条件多请求 + DB 查重复 | 只分析不动手压 |
| 外部 API | 本地 mock（绝不直推生产） | 真环境直连 |

## 测试矩阵格式

改前先列，改后逐条回填：

```
## 测试用例

| ID | 测试项 | 方法 | 预期 | 状态 |
|----|--------|------|------|------|
| T1 | 正常上传 CSV | 点击上传→选文件→预览 | 预览区显示 10 行数据 | ✅ |
| T2 | 空文件 | 上传空 CSV | 弹出错误提示「文件无数据」 | ✅ |
| ... | ... | ... | ... | ... |
```

基线用例（上一版通过的）必须在改完后重跑，确认没搞坏。