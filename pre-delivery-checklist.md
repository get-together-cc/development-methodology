# 交付前清单（⑤绿→部署之间逐项确认）

> 部署前逐项检查，一项不过不交付。

## 代码和版本

- [ ] 所有变动档已 git add + commit，`git diff` 确认只改该改的行
- [ ] 版本号已 bump（文件头 Version + 常量双保险）
- [ ] CHANGELOG 已更新（含废弃项移除→对应三清单 upgrade delta）
- [ ] 语法检查通过：`php -l` 所有改动档
- [ ] 三清单 upgrade delta 已更新：废弃文件/定时任务/选项已清理
- [ ] JS/CSS 版本参数已 bump（防浏览器缓存旧文件）

## 部署

- [ ] 部署四步：scp /tmp → wc -c 校验 → mv 原子替换 → md5 核对
- [ ] 旧文件/旧 .bak 已清理（服务器不留备份文件）
- [ ] FPM 已重启（OPcache 清空）
- [ ] 对象缓存已清理（Memcached/Redis flush）
- [ ] 服务器端验证：`ls -la` + `file` 确认文件存在、内容完整

## 测试

- [ ] UI 全路径无头浏览器真实点击测试完成（上传/预览/执行/二次上传/取消/错误文件）
- [ ] 自适应/断点测试完成（CSS 改动时）
- [ ] 基线复验：重跑上一版通过的测试用例，全部绿
- [ ] 生产只观察原则：改动不上生产测试，生产只需验证 200 + 正确渲染

## 审核

- [ ] 多模型审核闭环记录（发现问题数/已修复/再审确认）
- [ ] 测试矩阵逐条回填
- [ ] 并发/幂等已验证（脚本压测或在审核轮已确认收敛）

## 记录

- [ ] 维护记录三步：审计日志 + 任务总结 + 索引
- [ ] 交付总结附审核结果清单（不写「全部完成」）
- [ ] 项目类型特定：Go 服务需额外确认交叉编译 + 本地 mock + 灰度双跑